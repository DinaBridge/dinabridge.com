<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8" />
  <title>About DinaBridge</title>

  <style>
    .about-hero {
      background: linear-gradient(135deg, var(--blue), var(--teal));
      border-radius: 28px;
      padding: 48px;
      display: grid;
      grid-template-columns: 1fr 1fr;
      gap: 40px;
      color: #fff;
    }

    .about-left h2 {
      font-family: var(--serif);
      font-size: clamp(32px,4vw,44px);
      line-height: 1.1;
      margin-bottom: 1rem;
    }

    .about-left p {
      margin-bottom: 1.5rem;
      color: rgba(255,255,255,0.9);
    }

    .about-grid {
      display: grid;
      grid-template-columns: repeat(4, 1fr);
      gap: 14px;
    }

    .avatar {
      aspect-ratio: 1/1;
      border-radius: 18px;
      background: rgba(255,255,255,0.08);
    }

    @media (max-width: 900px) {
      .about-hero {
        grid-template-columns: 1fr;
      }
    }
  </style>
</head>

<body>

<nav>
  <!-- your existing nav here -->
</nav>

<main id="main">

  <!-- ✅ YOUR SECTION (ONLY ONCE, INSIDE MAIN) -->
  <section class="section">
    <div class="container">
      <div class="about-hero">

        <div class="about-left">
          <h2>
            Built for production systems.<br>
            Trusted in partner-led environments.
          </h2>

          <p>
            DinaBridge works with enterprises and consulting partners to deliver
            production-grade observability, search, and data systems.
          </p>

          <a href="./contact.html" class="primary-btn">Contact Us</a>
        </div>

        <div class="about-grid">
          <div class="avatar"></div>
          <div class="avatar"></div>
          <div class="avatar"></div>
          <div class="avatar"></div>
          <div class="avatar"></div>
          <div class="avatar"></div>
          <div class="avatar"></div>
          <div class="avatar"></div>
        </div>

      </div>
    </div>
  </section>

</main>

</body>
</html>
